% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confirm.R
\name{prune_unconfirmed}
\alias{prune_unconfirmed}
\title{Collapse Splits That Did Not Confirm}
\usage{
prune_unconfirmed(
  tree,
  confirmation,
  alpha = 0.05,
  which = c("adjusted", "raw"),
  outcome = "any"
)
}
\arguments{
\item{tree}{The \code{ctreeMI} object that was confirmed.}

\item{confirmation}{A \code{ctreeMI_confirm} object from \code{\link[=confirm_ctreeMI]{confirm_ctreeMI()}}.}

\item{alpha}{Level at which a split counts as confirmed. Default 0.05.}

\item{which}{Which p-value to use: \code{"adjusted"} (default) or \code{"raw"}.}

\item{outcome}{For a multivariate outcome, the name of the outcome whose
tests govern pruning, or \code{"any"} (default) to retain a split confirmed
for any outcome, or \code{"all"} to require every outcome.}
}
\value{
A \code{ctreeMI} object with unconfirmed splits collapsed. Its
\code{ctreeMI_info} attribute gains \code{confirmed}, a data frame recording which
splits were retained and why.
}
\description{
Takes a discovery tree and its confirmation, and returns the tree with
every split that failed to confirm collapsed into a terminal node.
}
\details{
Pruning is bottom-up and a split is collapsed only once all of its own
internal descendants have been collapsed, matching \code{\link[=prune_stackM]{prune_stackM()}}. A
split whose p-value could not be computed, because a child node fell
below \code{min_node} in some imputation, is treated as unconfirmed; the tree
offers no evidence for it on the confirmation data.

The result is the partition the confirmation data supports. Its terminal
nodes can be reported without the caveat that attaches to the discovery
tree, since the splits defining them were tested on data the tree had not
seen.
}
\examples{
\dontrun{
res <- discover_confirm(y ~ ., data = d, m = 30, seed = 1)
pruned <- prune_unconfirmed(res$tree, res$confirmation)
plot(pruned)
}
}
\seealso{
\code{\link[=confirm_ctreeMI]{confirm_ctreeMI()}}, \code{\link[=discover_confirm]{discover_confirm()}}
}
