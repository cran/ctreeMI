% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confirm.R
\name{discover_confirm}
\alias{discover_confirm}
\title{Discover a Tree and Confirm It on Held-Out Data}
\usage{
discover_confirm(
  formula,
  data,
  prop = 0.5,
  m = 30L,
  strata = NULL,
  seed = NULL,
  alpha = 0.05,
  mice_args = list(),
  ...
)
}
\arguments{
\item{formula}{Model formula, as for \code{\link[=ctree_stacked]{ctree_stacked()}}.}

\item{data}{A data frame containing missing values.}

\item{prop}{Proportion assigned to discovery. Default 0.5.}

\item{m}{Number of imputations for each half. Default 30.}

\item{strata}{Optional stratification column for the split.}

\item{seed}{Optional integer seed. The confirmation imputation uses
\code{seed + 1L} so that the two halves are imputed with different streams.}

\item{alpha}{Nominal level for the discovery tree. Default 0.05.}

\item{mice_args}{A list of additional arguments passed to
\code{\link[mice:mice]{mice::mice()}} for both halves.}

\item{...}{Further arguments passed to \code{\link[=ctree_stacked]{ctree_stacked()}}.}
}
\value{
An object of class \code{"ctreeMI_dc"} containing \code{tree} (the
\code{ctreeMI} fit), \code{confirmation} (a \code{ctreeMI_confirm} object), \code{split}
(the \code{ctreeMI_split} object), and the two \code{mids} objects as
\code{imp_discover} and \code{imp_confirm}.
}
\description{
A single call that splits the data, imputes each half separately, fits
the Stack/M tree on the discovery half, and tests the resulting
partition on the confirmation half. Equivalent to calling
\code{\link[=split_holdout]{split_holdout()}}, \code{\link[mice:mice]{mice::mice()}} twice, \code{\link[=ctree_stacked]{ctree_stacked()}} and
\code{\link[=confirm_ctreeMI]{confirm_ctreeMI()}} in sequence.
}
\details{
Imputing the halves separately is deliberate. A single imputation of the
full data lets each half's outcomes inform the other's imputed
predictors, and the confirmation test then no longer sees independent
data. The cost is that each imputation model is fitted to half the
observations. For most designs this is a reasonable trade; for small
samples it may not be, and the user should judge.
}
\examples{
\dontrun{
set.seed(11)
n <- 800
d <- data.frame(x1 = rnorm(n), x2 = factor(sample(letters[1:3], n, TRUE)))
d$y <- rnorm(n) + 1.5 * (d$x1 > 0.3) + (d$x2 == "a")
d$x1[sample(n, 120)] <- NA
d$x2[sample(n, 80)]  <- NA

res <- discover_confirm(y ~ x1 + x2, data = d, m = 20, seed = 11)
res$tree
res$confirmation
}
}
\seealso{
\code{\link[=split_holdout]{split_holdout()}}, \code{\link[=confirm_ctreeMI]{confirm_ctreeMI()}}, \code{\link[=ctree_stacked]{ctree_stacked()}}
}
